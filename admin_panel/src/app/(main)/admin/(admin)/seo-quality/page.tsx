import SeoQualityClient from './_components/seo-quality-client';

export const metadata = {
  title: 'SEO Kalite | GoldMoodAstro Admin',
};

export default function SeoQualityPage() {
  return <SeoQualityClient />;
}
