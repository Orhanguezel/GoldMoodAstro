import ServiceTemplatesClient from './service-templates-client';

export const metadata = {
  title: 'Hizmet Şablonları | Yönetim Paneli',
};

export default function ServiceTemplatesPage() {
  return <ServiceTemplatesClient />;
}
