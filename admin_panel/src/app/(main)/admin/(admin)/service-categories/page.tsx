import ServiceCategoriesClient from './service-categories-client';

export const metadata = {
  title: 'Hizmet Kategorileri | Yönetim Paneli',
};

export default function ServiceCategoriesPage() {
  return <ServiceCategoriesClient />;
}
