# MOBİL TAM DENETİM ÇEKLİSTİ — 2026-07-04

> **Kapsam:** `mobile/app` (Expo SDK 54, RN 0.81, expo-router 6) — web frontend paritesi,
> danışan + danışman panelleri, iOS/Android store hazırlığı, backend/admin destek yüzeyi.
> **Denetim yöntemi:** 3 paralel derin tarama (mobil repo, web frontend envanteri, backend+admin).
> **Genel durum:** Danışan tarafı içerik paritesi ~%85 ve gerçek API'ye bağlı (mock değil).
> Ancak **4 kritik blocker** var (LiveKit mock, IAP stub, danışman paneli yok, şifre sıfırlama mock)
> ve mobil, Temmuz'da web'e giren dalgadan (interval booking, favoriler/presence, medya mesajları) habersiz.

---

## ÖZET DURUM TABLOSU

| Alan | Durum |
|---|---|
| Danışan içerik paritesi (tarot/kahve/rüya/numeroloji/sinastri/yıldızname/harita/burçlar/blog/karne/KVKK) | ✅ ~%85 — gerçek API |
| Sesli/görüntülü görüşme (LiveKit) | 🔴 KIRIK — metro mock tüm build'lerde aktif |
| Danışman paneli | 🔴 YOK — %0 |
| IAP (iOS abonelik/kredi) | 🔴 STUB — App Store engeli |
| Şifre sıfırlama | 🔴 MOCK |
| Interval (esnek) randevu | 🔴 Eski `/slots` endpoint'inde (INT-T8 bloklu) |
| Favoriler | 🟡 Lokal AsyncStorage — backend `/me/favorites` hazır ama bağlanmamış |
| Presence / medya mesajları (VMSG) | 🔴 Mobilde hiç yok — backend hazır |
| Push (FCM/APNs) | 🟡 Android tamam, iOS plist/APNs eksik |
| Store yapılandırma (iOS) | 🟡 Photo library izni, privacy manifest, GoogleService-Info.plist eksik |
| i18n (tr/en/de) | 🟡 Altyapı sağlam, ~6 ekranda hardcoded TR sızıntısı |
| Tema (design tokens runtime) | ✅ Tam — backend fetch + fallback |
| KVKK (export + hesap silme) | ✅ Tam — Apple zorunluluğu karşılanıyor |
| Apple Sign In | ✅ Tam (backend JWKS doğrulaması dahil) |

---

## A. KRİTİK BLOCKER'LAR (store'a çıkmadan önce ŞART)

- [x] **A1 — LiveKit mock'unu build profiline bağla.** `mobile/app/metro.config.js:14-19` `@livekit/react-native`'i **koşulsuz** `src/mocks/livekit-react-native-mock.tsx`'e resolve ediyor → `registerGlobals()` no-op, `VideoView` boş `<View>`. **Mevcut config ile hiçbir production build'inde sesli/görüntülü görüşme çalışmaz.** Çözüm: alias'ı env flag'e bağla (`EXPO_PUBLIC_LIVEKIT_MOCK=1` sadece Expo Go/dev), preview/production'da gerçek modül. Dev-client build ile gerçek cihazda doğrula. **Tamamlandı:** alias `EXPO_PUBLIC_LIVEKIT_MOCK=1` bayrağına bağlandı; preview/production env'lerinde bayrak yok, gerçek modül resolve edilir.
- [x] **A2 — Şifre sıfırlamayı gerçek API'ye bağla.** `mobile/app/app/auth/forgot.tsx:143-144` sahte `setTimeout(1500)` + her zaman "gönderildi" Alert'i. Web'deki `/forgot-password` + `/password-reset` akışının kullandığı backend endpoint'lerine bağla (deep link ile reset ekranına dönüş dahil — bkz. G4). Apple, çalışmayan özelliği red sebebi sayar. **Tamamlandı:** forgot ekranı `/auth/password-reset/request` gerçek endpoint'ini çağırıyor; deep link/reset dönüş kararı G4/F6'da açık kaldı.
- [x] **A3 — IAP stub'dan çık (iOS zorunlu).** `mobile/app/src/lib/iap.ts` her zaman `ok:false` döner; `react-native-iap`/RevenueCat bağımlılığı **yok**. iOS'ta dijital abonelik Iyzipay WebView ile satılamaz (Apple 3.1.1 reddi). Backend receipt doğrulama **hazır** (`/subscriptions/verify-receipt`, Apple prod+sandbox, Google RTDN). Karar: RevenueCat vs `react-native-iap` → entegre et → gerçek receipt gönder. **Tamamlandı:** Expo SDK 54 ile uyumlu `expo-iap` entegre edildi; abonelik ürünü store'dan çekiliyor, iOS receipt / Android purchase token backend `/subscriptions/verify-receipt` endpoint'ine gönderiliyor ve doğrulama sonrası transaction finalize ediliyor. Sandbox cihaz testi J6'da yapılacak.
- [x] **A4 — `purchase.ok` kontrolsüz boş makbuz bug'ı.** `mobile/app/app/(tabs)/profile/subscription.tsx:352-367` stub `ok:false` dönse bile `receipt: ''` ile `verifyReceipt`'e devam ediyor. A3'ten bağımsız olarak bu mantık hatasını hemen düzelt (satın alma başarısızsa akışı durdur + kullanıcıya mesaj). **Tamamlandı:** `purchase.ok` veya `receipt` yoksa akış duruyor ve kullanıcıya hata gösteriliyor.
- [x] **A5 — Prod API URL güvencesi.** `mobile/app/app.json` `extra.apiUrl = http://localhost:8094/api` sabit; prod yönelim yalnızca `eas.json` env'inden geliyor. EAS dışı build localhost'a bakar → boş uygulama. `extra.apiUrl`'ü prod'a çevir, dev'i `.env`/development profile'a taşı. **Tamamlandı:** app.json default API/site URL canlı domain'e alındı; local dev `.env`/development env üzerinden kalıyor.
- [x] **A6 — Auth token'ı SecureStore'a taşı.** `mobile/app/src/lib/storage.ts` JWT'yi düz AsyncStorage'da tutuyor (`gma.auth.token.v1`); `expo-secure-store` kurulu ama import bile edilmiyor. README/CURSOR-NOTES ile kod çelişiyor. Token okuma/yazmayı SecureStore'a geçir (migration: ilk açılışta AsyncStorage→SecureStore taşı + eskisini sil). **Tamamlandı:** auth/refresh token SecureStore'a taşındı; ilk okumada legacy AsyncStorage token'ı migrate edilip siliniyor.

## B. DANIŞMAN PANELİ (mobilde %0 — en büyük parite boşluğu)

> Mevcut durum: `storage.getUserRole()` rolü saklıyor (`storage.ts:62-66`) ama `app/` içinde **tek bir rol dallanması yok** — danışman girişi yapan da müşteri UI'ı görüyor. Web'deki `ConsultantDashboard` 12 tab'lı tam panel. Backend endpoint'leri hazır (`consultant_self`), yeni API yazılması gerekmiyor.

- [x] **B1 — Rol tabanlı routing.** Login sonrası `role === 'consultant'` ise danışman tab grubu / segment'ine yönlendir (`app/(consultant)/` expo-router grubu öner). Profil ekranında role göre menü. **Tamamlandı:** `app/(consultant)/` route group ve `/consultant` sekmeli danışman alanı eklendi; login/Apple login consultant role için panele yönleniyor, profil ekranında yalnız consultant role için "Danışman Paneli" menüsü görünüyor.
- [x] **B2 — Danışman özet (overview):** aylık seans, kazanç, rating, favori sayısı (web: `ConsultantDashboard.tsx` OverviewPanel karşılığı). **Tamamlandı:** mobil danışman paneli `/me/consultant/stats` endpoint'ine bağlandı; aylık seans, kazanç, rating, yorum, favori, toplam seans, bekleyen randevu/mesaj ve müsaitlik kartları canlı veriden gösteriliyor.
- [x] **B3 — Randevu yönetimi:** gelen randevular, kabul/red, günün programı (web BookingsPanel karşılığı). **Tamamlandı:** mobil danışman randevu sekmesi `/me/consultant/bookings` ile canlı liste gösteriyor; Gelen/Bugün/Tümü filtreleri, randevu kartları, müşteri mesajı, media/service bilgisi ve `approve`/`reject` aksiyonları eklendi.
- [x] **B4 — Müsaitlik yönetimi:** haftalık çalışma saatleri + `slot_minutes` + tek-gün override + molalar (`consultant_time_blocks`). Web `AvailabilityPanel.tsx` referans; interval altyapısı (INT-T1..T7) backend'de canlı. **Tamamlandı:** mobil danışman paneline Müsaitlik sekmesi eklendi; haftalık çalışma saatleri/slot süresi `/me/consultant/availability` ile okunup kaydediliyor, tek gün aç/kapat `/availability/day` ile çalışıyor. Eksik backend route'ları tamamlandı: `/me/consultant/time-blocks` list/create/delete; mobil mola/blok ekleme-silme bu tabloya bağlı ve interval booking çakışma kontrolü tarafından kullanılıyor.
- [x] **B5 — Cüzdan/kazanç:** bakiye, transaction listesi, para çekme talebi. **Dikkat:** `POST /me/consultant/wallet/withdraw` KYC onaysız `403`, aylık payout cycle'da `429 too_soon` + `next_request_at` döner — ikisini de UI'da ele al. Komisyon oranını hardcode etme; `GET /settings/commission` / site_settings'ten çek (%15→%30 date-aware geçiş var). **Tamamlandı:** mobil danışman paneline Cüzdan sekmesi eklendi; `/me/consultant/wallet` ve `/withdrawals` canlı gösteriliyor, para çekme talebi `/wallet/withdraw` ile oluşturuluyor. KYC 403, payout 429 `next_request_at`, minimum tutar, IBAN/hesap sahibi ve yetersiz bakiye hataları UI'da özel mesajlarla ele alınıyor; komisyon oranı hardcode edilmedi.
- [x] **B6 — KYC akışı:** belge yükleme (`POST /me/consultant/kyc/documents`, `/submit`) + durum gösterimi. Withdraw'un ön şartı. **Tamamlandı:** mobil danışman paneline KYC sekmesi eklendi; kimlik/şirket/banka bilgileri `/me/consultant` üzerinden kaydediliyor, belgeler `/me/consultant/kyc/documents` ile yükleniyor, `/submit` başvurusu ve durum/reddetme mesajı gösteriliyor.
- [x] **B7 — Danışman chat tarafı:** thread listesi + yanıt (web `MessagesPanel.tsx` karşılığı). **Tamamlandı:** mobil danışman paneline Mesajlar sekmesi eklendi; `/me/consultant/threads`, `/messages`, `/reply`, `/read` uçlarıyla thread listesi, konuşma detayı, okundu işareti ve yanıt gönderimi bağlandı.
- [x] **B8 — Ücretli medya mesaj yanıtlama:** gelen sesli/görüntülü soruları listele + kayıtla yanıtla (web `MediaMessagesPanel.tsx`; SLA cron'u `media-message-sla.ts` backend'de çalışıyor — süre aşımı danışmana yansır). **Tamamlandı:** mobil danışman paneline Medya sekmesi eklendi; `/me/consultant/media-messages` listesi, soru dosyası oynatma/izleme, sesli/video yanıt kaydı, storage upload ve `/reply` gönderimi bağlandı.
- [x] **B9 — Danışman profil + hizmet yönetimi:** profil düzenleme, hizmetler (çok dilli tr/en/de, media_type audio/video) — web `ServicesPanel.tsx` karşılığı. (Mobilde v1'de salt-görüntüleme + webview yönlendirme kabul edilebilir; karar ver.) **Tamamlandı:** mobil danışman profil sekmesi gerçek düzenleme ekranına çevrildi; bio/ücret/süre/video/müsaitlik alanları `/me/consultant` ile kaydediliyor, hizmet listesi ve audio/video hizmet oluşturma/aktif-pasif/silme işlemleri `/me/consultant/services` uçlarına bağlandı.
- [x] **B10 — Görüşmeye katılım:** danışman `call/[bookingId]`'e girebilmeli (web'de 403 fix'i `a99092e` ile yapıldı; mobil call ekranı rol kontrolüyle test edilmeli). **Tamamlandı:** mobil call ekranı danışman rolünde karşı tarafı danışan adı/e-postasıyla gösteriyor; booking detail ve LiveKit token akışı danışman erişimiyle çalışıyor, bağlantı hataları görünür mesaj + tekrar dene/mesaj seçenekleriyle ele alınıyor.
- [x] **B11 — Gelen çağrı bildirimi:** randevu saati / request-now geldiğinde danışmana push + tap→call ekranı. (Tam CallKit/VoIP push v2'ye bırakılabilir; v1'de normal FCM data push + foreground banner yeterli — G5 ile birlikte.) **Tamamlandı:** request-now bildirimi danışman randevu paneline yönleniyor, kabul sonrası danışman call ekranına geçiyor ve danışana `incoming_call` push gidiyor; randevu başlangıç cron'u iki tarafa `incoming_call` data push'u gönderiyor.
- [x] **B12 — Presence heartbeat:** `POST /me/consultant/heartbeat` mobilden atılmalı (FAVORI-PRESENCE çeklistinde "mobil ayrı görev" olarak not düşülmüş). App foreground'dayken interval, background'da durdur. **Tamamlandı:** danışman tab layout'u app foreground'da hemen heartbeat atıyor ve 60 sn interval ile yeniliyor; background/unmount durumunda interval durduruluyor.
- [x] **B13 — Reviews + karne:** danışmanın aldığı yorumları görme/yanıtlama (web `ReviewsPanel.tsx`). **Tamamlandı:** mobil danışman paneline Yorumlar sekmesi eklendi; `/me/consultant/reviews` listesi filtrelerle gösteriliyor ve yanıtsız yorumlara `/reply` üzerinden danışman yanıtı gönderilebiliyor.

## C. DANIŞAN PARİTESİ — Temmuz dalgası (web'de var, mobilde yok)

- [x] **C1 — Interval (esnek) randevuya geçiş (INT-T8).** Mobil hâlâ `GET /consultants/:id/slots` (eski) kullanıyor (`mobile/app/src/lib/api.ts:324`). Yeni: `GET /consultants/:id/availability?date=&duration=` → web `TimelinePicker` mantığının RN karşılığı (süre seçimi + timeline). Eski `/slots` endpoint'i mobil geçene kadar bilinçli açık bırakılmış — geçiş sonrası backend'de kapatma görevini de planla. **Tamamlandı:** mobil danışman detayı `/consultants/:id/availability` kullanıyor; seçili hizmet süresi ve `service_id` gönderiliyor, `starts` mevcut saat grid'ine interval başlangıçları olarak bağlandı.
- [x] **C2 — Favorileri backend'e taşı.** Mobil `useFavorites` **AsyncStorage'da lokal** tutuyor; backend `POST/DELETE /me/favorites/:consultantId`, `GET /me/favorites`, `/ids` hazır. Geçişte lokal favorileri backend'e migrate et (ilk login'de sync). Favori danışman online push'u (`favorite_online`) zaten cron'da — mobil bundan ancak backend favorilerle yararlanır. **Tamamlandı:** `favoritesApi` eklendi; hook remote-first çalışıyor, legacy AsyncStorage favorilerini ilk başarılı yüklemede backend'e migrate ediyor; favoriler ekranı `/me/favorites` kullanıyor.
- [x] **C3 — Presence (online) göstergesi.** Danışman kart + detayında `is_online` yeşil nokta; offline ise arama/medya soru davranış değişikliği (web `ConsultantDetail.tsx:173` referans). **Tamamlandı:** `Consultant.is_online` tipe eklendi; kart ve detay yeşil noktada `is_online` öncelikli kullanıyor, anlık görüşme offline durumda engelleniyor.
- [x] **C4 — Ücretli medya soru mesajları (VMSG) danışan tarafı.** Sesli/görüntülü soru gönderme (`/me/media-messages`, `audio_price`/`video_price`) + yanıtları dinleme/izleme. Web: `MediaQuestionModal.tsx` + dashboard `media` tab'ı. **Tamamlandı:** mobil `/media-messages` ekranı eklendi; müşteri mesajları `/me/media-messages` ile listeleniyor, profil menüsünden erişiliyor, sesli yanıt auth header'lı `expo-audio` oynatıcıyla dinleniyor, video yanıt `expo-video` native controls ile izleniyor. Danışman detayında `media-settings` okunuyor; audio/video soru kayıtları `media_messages` bucket'a upload edilip `/me/media-messages` ile oluşturuluyor.
- [x] **C5 — Booking'de media_type (audio/video) seçimi.** Web booking sayfasında var (`booking/page.tsx:51`); mobil checkout'ta hizmet media_type'a göre seçim/force mantığını doğrula, yoksa ekle. Video seansta fiyat farkı (`video_session_price`) yansımalı. **Doğrulandı:** danışman detayında hizmet `media_type` audio/video/both mantığı uygulanıyor, checkout `media_type` gönderiyor; hizmetsiz video seansta `video_session_price` kullanılıyor.
- [x] **C6 — `packages/index.tsx` statik upsell'i dinamikleştir.** Sabit kart yerine gerçek `credits/packages` + `subscriptions/plans` fiyatlarını çek. **Tamamlandı:** ekran `credits/packages` ve `subscriptions/plans` çağırıyor; kredi paketleri gerçek fiyatla listeleniyor, satın alma mevcut kredi checkout akışına bağlandı, premium kart abonelik yönetimine yönleniyor.
- [x] **C7 — Ünlüler ekranını dinamikleştir (opsiyonel).** `unluler/index.tsx` statik liste; web de `lib/zodiac/celebrities.ts` kullanıyor — parite bozuk değil ama içerik CMS'e taşınırsa iki taraf birlikte güncellenmeli. **Doğrulandı:** web ve mobil aynı statik arşiv deseninde; CMS/API sözleşmesi olmadığı için dinamikleştirme kapsam dışı bırakıldı. Mobil ekrandaki başlık ve "Tümü" filtresi i18n'e bağlandı.
- [x] **C8 — Abonelik self-servis yönetimi.** Mobilde plan görüntüleme var; iptal/yükseltme akışını IAP entegrasyonuyla (A3) birlikte tasarla. (Not: web'de de zayıf — parite hedefi birlikte netleşsin.) **Tamamlandı:** IAP aboneliklerinde "Yönet/İptal" App Store / Play abonelik merkezine deep link açıyor; "Satın Alımları Geri Yükle" eklendi ve restore edilen Apple/Google abonelikleri backend receipt doğrulamaya bağlanıyor. Gerçek sandbox satın alma/restore/iptal QA'sı J6'da kalıyor.
- [x] **C9 — Rectification (harita düzeltme) hizmet şablonu** web'e eklendi (`2b663b0`) — mobil hizmet listesinde göründüğünü doğrula (servisler API'den geldiği için muhtemelen otomatik; sadece QA). **Doğrulandı:** `birth_chart|rektifikasyon` template seed'i ve approved birth_chart danışmanları için `consultant_services` seed'i mevcut; public `/consultants/:id/services` aktif servisleri döndürüyor ve mobil danışman detay ekranı servis ad/açıklama/süre/fiyat alanlarını API'den render ediyor.
- [x] **C10 — RelaxMixer stem sesleri (FAZ 35 T35-1).** 8 adet `.m4a` stem dosyası + `stemAssets.ts` require hâlâ bekliyor — mixer UI var ama ses yok. **Doğrulandı:** `pad/rain/wind/water/chimes/forest/binaural/crackle.m4a` mevcut ve `stemAssets.ts` içinde require ediliyor.

## D. ÖDEME / IAP AYRINTILARI

- [x] **D1 — iOS kredi paketi satışı kararı.** `credits/buy` yalnız Iyzipay; iOS'ta tüketilebilir dijital kredi de IAP kapsamında (Apple 3.1.1). Karar: iOS'ta kredi satışını IAP consumable olarak ekle **veya** iOS'ta kredi satın almayı gizle (reader-app istisnası yok). Backend'e IAP kredi doğrulama endpoint'i gerekir (şu an YOK — G bölümü). **Tamamlandı:** native iOS/Android kredi satın alma CTA'ları IAP entegrasyonu gelene kadar bloke edildi; Iyzipay kredi checkout yalnız web ortamında çalışır.
- [x] **D2 — Android ödeme yolu netleştir.** Google Play da dijital içerikte Play Billing zorunlu tutar — Iyzipay WebView Android'de de politika riski. RevenueCat seçilirse iki platform tek entegrasyonla çözülür; danışman seansı (gerçek zamanlı kişisel hizmet) IAP kapsamı DIŞINDA kalır, Iyzipay orada kalabilir. Bu ayrımı (dijital içerik=IAP, kişisel hizmet=Iyzipay) dokümante et. **Tamamlandı:** `mobile/MOBILE-STRATEGY.md` P2 bölümünde dijital içerik/abonelik/kredi=IAP/Play Billing, canlı birebir seans=Iyzipay ayrımı dokümante edildi.
- [x] **D3 — Ödeme dönüş URL pattern'lerini sabitle.** `booking/payment.tsx:59-66` `/siparis/basarili` ve `/sepet?payment=failed` string match'ine dayanıyor — backend/web bu path'leri değiştirirse mobil sessizce kırılır. Callback URL'lerini site_settings'ten veya sabit kontratla yönet. **Tamamlandı:** `mobile_payment_success_patterns` / `mobile_payment_failure_patterns` site_settings seed'leri eklendi; mobil ödeme WebView bu pattern'leri runtime'da çekiyor ve seed yoksa güvenli fallback listesiyle çalışıyor.
- [x] **D4 — T41-1 (`/auth/me`'ye `is_premium` + subscription özeti).** REQUESTS.md'de "bekliyor"; `usePremium` `/subscriptions/me` fallback'inde. Backend'e ekle → mobil premium gating (reklamsız pro, FAZ 41) doğru çalışsın. **Doğrulandı:** backend `auth/me` yanıtı `is_premium` ve `subscription` döndürüyor; mobil `usePremium` bu alanları kullanıyor.

## E. PUSH & ÇAĞRI BİLDİRİMLERİ

- [ ] **E1 — iOS push zinciri kur.** `GoogleService-Info.plist` yok, app.json iOS `googleServicesFile` tanımsız. Karar + uygulama: FCM-üzerinden-APNs (plist + Firebase iOS app kaydı) veya doğrudan APNs key. Backend `profiles.fcm_token` FCM bekliyor → iOS token'ın FCM formatında gitmesi için Firebase kurulumu şart. **Kısmi hazırlık:** app config `expo.ios.googleServicesFile="./GoogleService-Info.plist"` olarak sabitlendi; `mobile/PUSH-SETUP.md` ve `mobile/app` `check:release` preflight script'i eklendi; Android `google-services.json` varlığı, iOS plist dosya varlığı ve release push bağımlılıkları otomatik denetleniyor. **Bloklu:** iOS için `mobile/app/GoogleService-Info.plist` ve Firebase/APNs iOS credential'ı repoda yok. Bu dosya sağlanmadan EAS iOS push zinciri tamamlandı işaretlenemez.
- [x] **E2 — FCM token unregister.** Backend'de `POST /firebase/register-token` var ama **unregister yok** — logout'ta eski cihaza push gitmeye devam eder (yanlış kullanıcıya bildirim riski). Backend endpoint + mobil logout entegrasyonu. **Tamamlandı:** `POST /push/unregister-token` eklendi; mobil logout öncesi best-effort çağırıyor ve lokal push token cache'i temizleniyor.
- [x] **E3 — Bildirim tiplerinin deep link haritasını genişlet.** `_layout.tsx:57-66` yalnız `booking_id` ve `screen==='bookings'` yönlendiriyor. Yeni tipler: `favorite_online` → danışman detay, `media_message_*` → medya mesaj ekranı, chat mesajı → thread, `booking_reminder` → call ekranı. **Tamamlandı:** handler `incoming_call`, `booking_reminder`, `favorite_online`, chat thread, eski booking/screen payload'ları ve `media_message_*` → `/media-messages` yönlendirmesini destekliyor.
- [ ] **E4 — Request-now (anlık görüş) çağrı akışı mobilde uçtan uca test.** Backend `request-now-timeout.ts` cron'u var; mobil `bookings/request-now` çağırıyor ama danışman tarafı mobilde olmadığından akış tek taraflı. B11 ile birlikte kapat. **Kısmi hazırlık:** push/deep-link route çözümleyici `notificationRoutes.ts` saf modülüne taşındı; `booking_requested_now`, `incoming_call`, `booking_reminder`, chat, favori ve medya mesaj yönlendirmeleri smoke test kapsamına alındı. **Bloklu:** danışman mobil paneli ve `incoming_call` push yönlendirmesi kod tarafında hazır; tam kapatma için gerçek danışan+danışman cihazlarında request-now kabul/timeout/call ekranı QA'sı gerekiyor.

## F. iOS / ANDROID STORE HAZIRLIĞI

- [x] **F1 — iOS `NSPhotoLibraryUsageDescription` ekle.** `expo-image-picker` kullanılıyor (avatar + kahve falı foto) ama izin metni yok → App Store reddi/crash. (Gerekirse `NSPhotoLibraryAddUsageDescription` de.) **Tamamlandı:** `NSPhotoLibraryUsageDescription` ve `NSPhotoLibraryAddUsageDescription` app config'e eklendi.
- [x] **F2 — iOS Privacy Manifest + App Privacy beyanı.** `PrivacyInfo.xcprivacy` / required-reason API beyanı görünmüyor (Apple 2024+ zorunlu). Expo config plugin'leriyle üret; App Store Connect "App Privacy" formunu (toplanan veri: hesap, doğum verisi, ödeme, foto) hazırla. **Tamamlandı:** `app.json` `privacyManifests` ile tracking=false, required-reason API beyanları ve toplanan veri tipleri eklendi; `mobile/APP-PRIVACY-DISCLOSURE.md` App Store Connect form özeti olarak hazırlandı. `expo config` manifesti doğruluyor.
- [x] **F3 — `buildNumber` (iOS) / `versionCode` (Android) başlangıç değerleri.** app.json'da tanımsız; `eas.json` `autoIncrement` + `appVersionSource: local` ile başlangıç değerlerini netleştir. **Tamamlandı:** iOS `buildNumber: "1"` ve Android `versionCode: 1` eklendi.
- [ ] **F4 — `eas.json` `submit.production` doldur.** Apple Team/App ID + Google service account. iOS native proje yok (EAS managed) — ilk `eas build -p ios` için Apple Developer hesabı hazır olmalı. **Kısmi:** secretsız alanlar dolduruldu (`bundleIdentifier`, `appName`, `sku`, `language`, Android `applicationId`, `track=production`, `releaseStatus=draft`); `bun run check:release` iOS `appleId`/`ascAppId`/`appleTeamId` eksiklerini fail ediyor. **Bloklu:** Apple hesap kimlikleri ve Google Play service account Expo credential'ı veya local key yok; gerçek submit için operasyon credential'ları gerekli.
- [x] **F5 — EAS Update (OTA) aç.** `updates.url` boş — şu an her düzeltme yeni store build'i demek. `eas update:configure` ile aç; runtimeVersion policy zaten `appVersion`. **Tamamlandı:** `updates.url` EAS projectId ile `https://u.expo.dev/...` adresine bağlandı.
- [ ] **F6 — Deep link altyapısı (universal/app links).** `apple-app-site-association` + `assetlinks.json` repoda YOK, nginx `/.well-known/` servis etmiyor (`nginx/goldmoodastro.conf` yalnız acme-challenge). Şifre sıfırlama e-postası, ödeme dönüşü ve paylaşım linkleri için gerekli. app.json'a `associatedDomains` + `intentFilters` ekle. **Kısmi:** app.json'a iOS `associatedDomains` ve Android HTTPS `intentFilters` eklendi; nginx standart AASA/assetlinks path'lerini frontend'e geçiriyor; `mobile/DEEP-LINK-SETUP.md` gerçek dosya template'lerini içeriyor; `bun run check:release` bu konfig/doküman varlığını denetliyor. **Bloklu:** geçerli `.well-known` dosyaları için Apple Team ID ve Android production signing SHA256 gerekiyor.
- [x] **F7 — Tablet kararı.** `supportsTablet:false` — iPad'de yayınlanmayacaksa bilinçli bırak, yayınlanacaksa layout QA. **Doğrulandı:** v1 store yayını telefon odaklı; `supportsTablet:false` bilinçli bırakıldı.
- [ ] **F8 — Store metadata hazırlığı.** Ekran görüntüleri (6.7"/6.5"/5.5" + tablet), açıklama (tr/en/de), anahtar kelimeler, yaş derecelendirmesi ("fal/astroloji eğlence amaçlıdır" ibaresi — 17+/18+ değerlendirmesi), destek URL'i, demo hesap (App Review için danışman + danışan test hesabı). **Kısmi:** `mobile/STORE-METADATA.md` içinde TR/EN/DE açıklama, keyword, kategori, yaş derecelendirme notu, destek/pazarlama/legal URL'leri ve demo hesap formatı hazırlandı; `bun run check:release` metadata dokümanının varlığını denetliyor. **Bloklu:** gerçek cihaz/store build screenshot'ları ve private demo hesap şifreleri operasyon sırasında oluşturulmalı.
- [x] **F9 — Legal içerik QA.** `legal/index.tsx` CMS'ten çekiyor (privacy/terms/cookies/kvkk/editorial/legal_notice) ama **mesafeli satış sözleşmesi anahtarı listede yok** ve boş içerik durumunda boş-durum UI'ı yok. Web'deki 10 legal slug ile eşitle; canlı DB'de içeriklerin dolu olduğunu doğrula. **Tamamlandı:** mobil legal anahtarları ortak `LEGAL_CMS_MODULE_KEYS` listesine alındı; `distance_sales` + `cancellation_refund` için TR/EN/DE seed eklendi ve canlı DB'ye `--no-drop --only=198` ile uygulandı. Canlı API doğrulaması: TR/EN/DE 9/9 legal sayfa dolu içerikle dönüyor.
- [x] **F10 — google-services.json `oauth_client` boş.** Google Sign-In mobilde yok (yalnız Apple + email). Web'de Google OAuth var → mobilde de Google Sign-In eklenecekse OAuth client oluştur; eklenmeyecekse bilinçli karar olarak not düş. **Karar:** mobil v1 giriş yöntemleri email + Apple Sign In; Google Sign-In mobil v1 kapsamı dışında, OAuth client üretildiğinde ayrı entegrasyon açılacak.

## G. BACKEND EKSİKLERİ (mobil için)

- [x] **G1 — App version / force-update endpoint'i.** Hiç yok. `site_settings`'e `mobile_min_version` + `mobile_latest_version` anahtarları → mobil açılışta kontrol + zorunlu güncelleme ekranı. (OTA açılsa bile native değişiklikler için şart.) **Tamamlandı:** `mobile_min_version`, `mobile_latest_version`, `mobile_update_url` seed edildi; mobil root açılışta min sürümü kontrol ediyor ve zorunlu güncelleme ekranı gösteriyor.
- [x] **G2 — FCM token unregister endpoint'i** (E2 ile aynı iş, backend ayağı). **Tamamlandı:** auth korumalı `/push/unregister-token` route'u mevcut kullanıcıdaki `fcm_token` alanını temizliyor.
- [x] **G3 — IAP kredi (consumable) doğrulama endpoint'i** (D1 kararına bağlı). Mevcut `verify-receipt` yalnız abonelik. **Tamamlandı:** `/credits/verify-receipt` Apple/Google consumable doğrulama endpoint'i eklendi; paket ürün kodu eşleşmesi, completed order kaydı ve order-id tabanlı idempotent kredi yazımı bağlandı. Mobil kredi ekranları `expo-iap` consumable satın alma → backend doğrulama → `finishTransaction(isConsumable: true)` akışına geçti. Store sandbox receipt testi J cihaz QA hattında yapılacak.
- [x] **G4 — Şifre sıfırlama mobil akışı.** Reset e-postasındaki link web'e gidiyor; mobil için deep link (F6) veya 6 haneli kod akışı kararı + gerekiyorsa endpoint uyarlaması. **Tamamlandı:** backend reset isteği `client=mobile` için `goldmoodastro://auth/reset?token=...` deep link üretip reset mailini tetikliyor; aynı endpoint 6 haneli kodu hash'li/süreli saklıyor ve confirm endpoint'i token veya `email+code` kabul ediyor. Mobil `forgot` ekranı gelen dev token ile `/auth/reset` ekranına geçiyor; prod'da mail deep link'i aynı ekrana açılıyor.
- [x] **G5 — Gelen çağrı push tetiği.** Booking başlangıcında/request-now kabulünde danışmana (ve danışana) `type: incoming_call` data push'u gönderen tetik yok — `booking-reminders.ts` yalnız 15 dk hatırlatma. VoIP push (PushKit) v2; v1'de normal FCM yeterli. **Tamamlandı:** `booking-reminders.ts` randevu başlangıç penceresinde müşteri ve danışmana `incoming_call` data push'u gönderiyor; request-now onayında danışana call push'u eklenmiş durumda.
- [x] **G6 — Global rate limit.** `@fastify/rate-limit` kayıtlı değil (yalnız tip tanımı var). Mobil istemci retry/backoff yapıyor; kötüye kullanım koruması için en azından auth + push + orders route'larına limit ekle. **Tamamlandı:** backend paketine `@fastify/rate-limit` eklendi, Fastify global 300/dk limit ile kaydedildi; mevcut auth route limitleri aktif hale geldi, push token/admin send ve orders/checkout uçlarına route bazlı limitler eklendi.
- [ ] **G7 — Eski `/slots` + slot-generator cron'u emekliye ayır** (C1 tamamlandıktan ve mobil store'da yayınlandıktan sonra — sıralamaya dikkat). **Kısmi hazırlık:** frontend ve mobil istemcilerde eski `/consultants/:id/slots` çağrıları kaldırıldı; frontend `SlotPicker/SlotGrid` ve `useGetConsultantSlots*` hook'u silindi, mobil `consultantsApi.slots()` helper'ı kaldırıldı. `rg` ile frontend/mobile istemci referansı kalmadığı doğrulandı. **Bloklu:** backend endpoint/cron kaldırma yayınlanmış eski mobil sürümleri kırmaması için store yayını sonrası yapılmalı.

## H. ADMIN PANEL EKSİKLERİ (mobil yönetimi)

- [x] **H1 — Banner formuna mobil placement'ları ekle.** Backend enum `mobile_welcome/mobile_home/mobile_call_end` destekliyor (`banners/schema.ts:34`) ama admin `banners/new/page.tsx:173-175` Select'inde yalnız `home_hero/home_sidebar/home_footer` var → admin mobil banner oluşturamıyor. **Doğrulandı:** admin formunda `mobile_welcome`, `mobile_home`, `mobile_call_end` seçenekleri ve TR/EN/DE çevirileri mevcut.
- [x] **H2 — KVKK silme talepleri görünürlüğü.** Silme cron otomatik işliyor (`account-deletion.ts`) ama admin'de bekleyen talepleri listeleyen sayfa yok — 7 günlük cooling-off penceresinde müdahale/denetim için basit liste ekle. **Tamamlandı:** `/api/admin/account-deletion-requests` liste endpoint'i eklendi; admin panelde `/admin/account-deletions` sayfası, pending/cancelled/completed filtresi, arama, süre göstergeleri ve sidebar bağlantısı eklendi.
- [x] **H3 — Kredi paketleri admin CRUD doğrulaması.** `subscription-plans/` sayfası var; kredi paketleri için eşdeğer yönetim sayfasının varlığını doğrula, yoksa ekle. **Tamamlandı:** `/api/admin/credit-packages` CRUD route'ları eklendi; oluştur/güncelle işlemleri `credit_package_i18n` TR/EN satırlarını da senkronluyor. Admin panelde `/admin/credit-packages` liste/form/silme ekranı ve sidebar bağlantısı eklendi.
- [x] **H4 — Push gönderim ekranına mobil hedefleme QA.** `notifications/send` var (all/specific); segment hedefleme (premium/free, danışman/danışan) FAZ 41 `target_segment` ile bağlanacaksa görev aç. **Tamamlandı:** manuel push backend'i `target_segment` kabul ediyor; `users`, `consultants`, `premium_users`, `free_users`, `users_without_booking`, `inactive_7d` segmentleri eklendi. Admin `/notifications/send` ekranında segment hedef seçimi ve dropdown'u bağlandı.
- [x] **H5 — `ui_mobile_i18n` düzenleme deneyimi.** Site-settings üzerinden override çalışıyor (019 seed); admin'de mobil i18n anahtarlarını aramalı/düzenlemeli bir görünüm (mevcut ui_* editörüne "mobile" filtresi) ekle. **Tamamlandı:** site-settings içine global “Mobil i18n” tab'ı eklendi; `ui_mobile_i18n` JSON'u TR/EN/DE seçimi, arama ve leaf string düzenleme ile yönetiliyor, kaydetme aynı site setting kaydına yazıyor.

## I. i18n / TEMA / KOD KALİTESİ

- [x] **I1 — Hardcoded Türkçe ekranları `t()`'ye geçir.** Tespit edilenler: `(tabs)/bookings.tsx` (244-321 arası ~7 string), `booking/payment.tsx:76,95`, `notifications.tsx:261`, `legal/index.tsx:97-104`. EN/DE kullanıcıda Türkçe sızıyor. Anahtarları `mobileI18n.ts` bundle'ına + `019_ui_mobile_i18n_seed.sql`'e ekle. **Tamamlandı:** belirtilen ekranlardaki görünür sabit metinler `t()` kullanımına alındı; TR/EN/DE fallback bundle anahtarları eklendi. Remote seed snapshot ayrıca yeniden üretilebilir.
- [x] **I2 — `t(key, 'Türkçe fallback')` desenini denetle.** Inline fallback TR olduğu için eksik anahtar EN/DE'de Türkçe gösterir; eksik anahtar taraması (basit script: bundle'da olmayan key kullanımı) ekle — web'deki `check:i18n` benzeri. **Tamamlandı:** `check-mobile-i18n.ts` mobil `t('...')` statik kullanımlarını bundle/seed anahtarlarına karşı tarıyor ve eksik key'de fail ediyor; inline Türkçe fallback borcunu uyarı olarak sayıyor. Eksik anahtarlar TR/EN/DE bundle'a eklendi ve `019_ui_mobile_i18n_seed.sql` yeniden üretildi.
- [x] **I3 — `api.ts`'deki 27+ `any`'yi tiple.** AGENTS.md "`any` yasak" kuralı ihlalde; en azından reading/tarot/synastry dönüş tiplerini tanımla. **Tamamlandı:** `mobile/app/src/lib/api.ts` profil, bildirim, burç, tarot/kahve/rüya/numeroloji/yıldızname/sinastri, kullanıcı arama, banner ve kampanya dönüşleri için somut tiplerle güncellendi; `api.ts` içinde `any` kullanımı kalmadı.
- [x] **I4 — ESLint ekle.** `lint` script'i yalnız `tsc --noEmit`; `eslint-config-expo` + temel kurallar. **Tamamlandı:** mobil pakete `eslint-config-expo` tabanlı `.eslintrc.cjs` eklendi; `lint` script'i artık `typecheck` + ESLint çalıştırıyor. Doğrulama 0 error ile geçti; mevcut kod tabanındaki `any`, import sırası, unused ve hooks uyarıları warning olarak görünür teknik borç bırakıldı.
- [x] **I5 — Refresh token akışı.** `storage.ts`'de get/set var ama hiç kullanılmıyor; 401'de kullanıcı sessizce logout ediliyor. Backend refresh endpoint'i varsa bağla; yoksa access token ömrünü ve UX'i bilinçli dokümante et. **Tamamlandı:** backend login/signup/social-login yanıtları `refresh_token` döndürüyor ve `/auth/token/refresh` mobil body `refresh_token` kabul ediyor; mobil login/register Apple akışı refresh token'ı SecureStore'a yazıyor. API client 401'de tek sefer refresh+retry yapıyor, başarısızsa mevcut oturum temizleme davranışına düşüyor.
- [x] **I6 — `expo-av` → `expo-audio` migrasyonu.** SDK 54'te deprecated; RelaxMixer bunun üzerinde. SDK 55 öncesi taşı. **Tamamlandı:** `expo-av` kaldırıldı; RelaxMixer, ses oynatma ve kayıt akışları `expo-audio`'ya taşındı. Auth header'lı video oynatma `expo-video` tabanlı `AuthVideoView` bileşenine geçti. `rg` ile `expo-av` referansı kalmadığı doğrulandı; `mobile/app` lint 0 error ile geçti (mevcut warning borcu korunuyor).
- [x] **I7 — Prod'da console log kısıtlama.** 65 `console.error/warn` çağrısı; `__DEV__` guard'lı logger'a sar. **Tamamlandı:** `mobile/app/src/lib/logger.ts` eklendi; uygulama ve shared mobile kaynaklarındaki `console.warn/error` çağrıları dev-only `logger.warn/error` kullanımına taşındı. `rg "console\\.(error|warn)" mobile/app/app mobile/app/src` boş dönüyor; `mobile/app` lint 0 error ile geçti (mevcut 501 warning teknik borcu korunuyor).
- [x] **I8 — Doküman senkronu.** MOBILE-STRATEGY.md `/api/v1` yazıyor (kod `/api`), CURSOR-NOTES token key `mh_access_token` yazıyor (kod `gma.auth.token.v1`), README "SecureStore" diyor (kullanılmıyor — A6 sonrası doğru olacak). Üçünü güncelle. **Tamamlandı:** README, app README, MOBILE-STRATEGY ve CURSOR-NOTES API base + SecureStore token bilgileri kodla hizalandı.
- [x] **I9 — Smoke test seti.** Hiç test yok. En azından kritik akışlar için (auth, booking create, api client unwrap) birim test + manuel QA checklist'i (FAZ 4 "EAS/Iyzipay/LiveKit/FCM smoke" zaten açık operasyon işi). **Tamamlandı:** `mobile/app/tests/api-smoke.test.ts` eklendi; API error/list/data unwrap, cache key stabilitesi, auth login/register/reset payloadları, booking create, Iyzipay order kontratı ve push/deep-link route kontratları 11 Bun smoke testiyle doğrulanıyor. `mobile/app` `test`/`test:smoke`, `check:i18n` ve `check:qa` script'leri eklendi; `bun run check:qa` i18n + smoke + lint zincirini 0 error ile geçti. Manuel cihaz QA için `mobile/SMOKE-QA-CHECKLIST.md` hazırlandı.

## J. YAYIN ÖNCESİ UÇTAN UCA QA (build alındıktan sonra)

- [ ] **J1 —** Gerçek iOS + Android cihazda dev-client build ile: kayıt→onboarding→doğum haritası→danışman seç→interval slot→Iyzipay ödeme→LiveKit sesli görüşme→review akışı uçtan uca. **Bloklu:** fiziksel iOS/Android cihaz + dev-client/store build + ödeme/call sandbox ortamı gerekiyor; repodan tamamlandı işaretlenemez.
- [ ] **J2 —** Aynı akışı **video** media_type ile (kamera izni, kamera değiştirme, hoparlör). **Bloklu:** kamera/mikrofon izinleri ve LiveKit video davranışı gerçek cihaz build'iyle doğrulanmalı.
- [ ] **J3 —** Danışman hesabıyla: login→randevu kabul→çağrıya katıl→cüzdan→withdraw 403/429 durumları. **Bloklu:** danışman test hesabı, canlı/dev backend verisi ve cihaz QA oturumu gerekiyor.
- [ ] **J4 —** Push: booking hatırlatma, favori online, medya mesaj bildirimi — tap yönlendirmeleri (E3). **Bloklu:** FCM/APNs credential'ları ve gerçek cihaz push testi gerekiyor; E1 iOS credential eksiği de bu testi etkiliyor.
- [ ] **J5 —** 3 dilde (tr/en/de) tüm sekmeler — Türkçe sızıntı taraması (I1/I2 sonrası). **Kısmi hazırlık:** `mobile/app` içine `check:i18n` ve `check:qa` script'leri eklendi; `bun run check:qa` statik i18n taraması + smoke + lint zincirini çalıştırıyor. Son doğrulama: 1220 key/locale OK, 11 smoke test geçti, lint 0 error. **Bloklu:** tam madde için tr/en/de gerçek cihaz ekran QA'sı gerekiyor.
- [ ] **J6 —** IAP sandbox: abonelik satın alma + restore purchases + iptal (App Store zorunlu "Restore" butonu dahil). **Bloklu:** App Store / Play sandbox hesapları, ürün kayıtları ve store build gerekiyor.
- [ ] **J7 —** Hesap silme + veri export akışı (Apple review'ün baktığı ilk yerlerden). **Bloklu:** gerçek hesapla cihaz üstünde KVKK export/silme e-postası ve cooling-off akışı doğrulanmalı.
- [ ] **J8 —** Uçak modu / zayıf ağ: cache davranışı, hata mesajları, retry. **Bloklu:** gerçek cihazda network throttling/uçak modu QA'sı gerekiyor; otomatik smoke kapsamı `mobile/SMOKE-QA-CHECKLIST.md` içine eklendi ve `bun run check:release` bu manuel QA dokümanının varlığını denetliyor.

---

## ÖNERİLEN SIRA (yol haritası)

1. **Sprint 1 — Blocker'lar:** A1-A6 + E1 + F1-F3 (uygulama "gerçekten çalışır" hale gelir, iOS build alınabilir).
2. **Sprint 2 — Temmuz paritesi:** C1-C5 + D3-D4 + E2-E3 + H1 (web ile aynı ürün olur).
3. **Sprint 3 — Danışman paneli:** B1-B13 + G5 (mobil, danışmanların da çalışabildiği kanal olur).
4. **Sprint 4 — IAP + store yayını:** A3 kararı uygulama + D1-D2 + F4-F9 + G1 + J1-J8.
5. **Sürekli:** I1-I9 kalite işleri sprint'lere serpiştirilir.

> **Güçlü yanlar (korunacak):** runtime tema token fetch + fallback, i18n bundle+remote merge mimarisi,
> KVKK export/silme tam, Apple Sign In (backend JWKS dahil) tam, push token kaydı + deep link temeli,
> GET cache/retry offline dayanıklılığı, token-tabanlı tasarım disiplini.
